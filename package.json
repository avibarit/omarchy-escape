{
  "name": "omarchy-escape",
  "version": "1.0.0",
  "description": "Omarchy tiling-shortcuts survival trainer (Electron native wrapper)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --check main.js && node --check preload.js && for f in js/*.js; do node --check \"$f\"; done && node test/tiling.test.js"
  },
  "devDependencies": {
    "electron": "^34.0.0"
  }
}
